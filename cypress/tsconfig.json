{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM"],
    "types": ["cypress", "node"],
    "outDir": "./dist/cypress" // Separate output directory for Cypress
  },
  "include": ["**/*.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", "dist", "cypress/dist"] // Exclude output directories
}